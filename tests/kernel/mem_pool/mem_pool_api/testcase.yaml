tests:
  kernel.memory_pool:
    filter: not CONFIG_HAS_COVERAGE_SUPPORT
    tags: kernel mem_pool
  kernel.memory_pool.coverage:
    filter: CONFIG_HAS_COVERAGE_SUPPORT
    extra_args: CONF_FILE=prj_cov.conf
    tags: kernel mem_pool
